# Screenshots

Images used by `README.md`, `README.pt-BR.md` and the CurseForge description.
Keep these exact filenames so the links resolve.

## Catalog

| Filename | What it shows |
|---|---|
| `roadmap.png` | Main `/mtrack` window — the roadmap, with the search box + filters at the top |
| `free-filter.png` | The free-text search box plus the filter toggles (current zone, owned, unavailable) |
| `category-filter.png` | The **Category** dropdown (Vendor / Reputation / Drop / Achievement / …) |
| `expansion-filter.png` | The **Expansion** dropdown |
| `glow.png` | A row (or a few) with the green **obtainable-now** glow |
| `row.png` | Close-up of one row: icon, name + status badge, source, zone + cost, current-standing line |
| `detail-panel.png` | The detail panel: **3D model preview**, source/zone/cost, action buttons (incl. **Edit data** in edit mode) |
| `waypoint.png` | A vendor mount's **Set waypoint to vendor** (native arrow/pin on the map) |
| `owned-toggle.png` | List with **Show owned** ticked (owned = colored, missing = gray) |
| `editmode.png` | The in-game **curation editor** (after `/mtrack enable edit` → "Edit data") |
| `minimap.png` | The minimap button + its tooltip |

> The old `zone-filter.png` was dropped — the current-zone toggle now lives alongside the
> other filters in `free-filter.png`.

## Tips

- Use a character with **mixed progress** (some renown/rep partway) so you get a
  good mix of statuses: 🟢 Ready, 🟡 Need currency, 🟠 Need requirement, 🔵 Farmable.
- Crop tightly and keep a consistent width (e.g. ~900px) for a clean README.
- PNG preferred. Avoid capturing personal account info if any is visible.
