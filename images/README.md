# Screenshots

Drop the screenshots referenced by the main `README.md` / `README.pt-BR.md` here.
Use the exact filenames below so the README links resolve, then uncomment the
image lines in the READMEs.

## Recommended shots

> **The roadmap now has a search box** (top of the window) and an optional **curation
> editor** (`/mtrack enable edit` → "Edit data" on the detail panel). The shots marked
> **⚠️ refresh** show an older layout and should be retaken. New shots are marked **🆕**.

| Filename | What to capture | Why it matters |
|---|---|---|
| `roadmap.png` | ⚠️ refresh — main `/mtrack` window, healthy list, **now including the search box** at the top | The hero shot — shows the whole value at a glance |
| `search.png` | 🆕 the roadmap with the **search box** filled in (e.g. a vendor or mount name), list filtered down | Shows the free-text search filter |
| `edit-panel.png` | 🆕 the **curation editor** window open (after `/mtrack enable edit` → "Edit data"), showing the fields/dropdowns | Shows the in-game curation/contributor tool |
| `glow.png` | ⚠️ refresh — a row (or a few) with the green **obtainable-now** glow, new row layout | The killer feature: "you can get this right now" |
| `row.png` | ⚠️ refresh — close-up of one row: icon, name + status badge, source line, zone + `Cost: N 🪙 Currency (have X)`, current-standing line, `›` | Shows the rich per-mount detail (no buttons now) |
| `detail-panel.png` | ⚠️ refresh — the detail panel open next to the list: **3D model preview**, name + badge, source/zone/cost, action buttons (now incl. **Edit data** when edit mode is on) | The big new feature — click a row to open it |
| `waypoint.png` | 🆕 a vendor mount's detail with **Set waypoint to vendor**, ideally showing the native arrow/pin on the map afterwards | Shows the new waypoint feature |
| `expansion-filter.png` | The **Expansion** dropdown open | Shows filtering by expansion |
| `zone-filter.png` | The **Zone** dropdown open (All zones / Current zone) | Shows the current-zone filter (dungeons & raids included) |
| `owned-toggle.png` | List with **Show owned** ticked (owned = colored, missing = gray) | Shows the owned/missing visual coding |
| `minimap.png` | The minimap button + its tooltip | Shows the launch button |

## Tips

- Use a character with **mixed progress** (some renown/rep partway) so you get a
  good mix of statuses: 🟢 Ready, 🟡 Need currency, 🟠 Need requirement, 🔵 Farmable.
- Crop tightly and keep a consistent width (e.g. ~900px) for a clean README.
- PNG preferred. Avoid capturing personal account info if any is visible.

After adding files, update the **Screenshots** section in both READMEs, e.g.:

```markdown
![Roadmap window](images/roadmap.png)
![Obtainable-now glow](images/glow.png)
```
